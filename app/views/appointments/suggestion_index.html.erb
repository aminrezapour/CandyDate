<div class="row">

    <div class="col-xs-2" id="amin6">
      <ul class="nav nav-pills nav-stacked">

      </ul>
    </div>

    <div class="col-xs-10">

      <div class="row" id="amin4">
          <h3><small>with: </small><%= @user.name %></h3>
      </div>

      <!-- <%= form_tag(user_available_index_path(current_user), method: :get) do %>
        <%= hidden_field_tag(:other_user, @user.id) %>

        <% @suggestions.each do |s| %>
          <div class="row">
            <div class="col-xs-10" id="amin8">
              <h3><%= s.name %></h3>
              <small><%= s.description %></small>
              <%= hidden_field_tag(:suggestion, s.id) %>
              <%= submit_tag("select", class: "btn btn-block btn-primary", id: "amin4") %>
            </div>
          </div>
        <% end %>

      <% end %> -->

      <%= form_tag(user_available_index_path(current_user), method: :get) do |f| %>
        <%= hidden_field_tag(:other_user, @user.id) %>

        <% @suggestions.each do |s| %>
            <%= check_box_tag 'suggestion_id', "#{s.id}" %>
            <div class="row">
              <div class="col-xs-10" id="amin8">
                <h3><%= s.name %></h3>
                <small><%= s.description %></small>
              </div>
            </div>
        <% end %>
        <%= submit_tag "Choose When", class: "btn btn-block btn-primary", id: "amin4" %>
      <% end %>

    </div>

</div>
